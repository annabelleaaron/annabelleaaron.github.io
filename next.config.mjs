/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",

    basePath: "/annabelleaaron.github.io",

    images: {
        unoptimized: true,
    },
    
    // distDir: 'dist',
};

export default nextConfig;
